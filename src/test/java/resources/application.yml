logging:
  level:
    root: INFO
    com.ninjaone.dundie_awards: INFO
    ExceptionLogger: ERROR

spring:
  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password: password
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
